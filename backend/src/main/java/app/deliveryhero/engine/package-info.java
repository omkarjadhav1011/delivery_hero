/** Game engine: one in-memory session per live game on its own command queue (HLD section 8.2). Null-checked by NullAway. */
@NullMarked
package app.deliveryhero.engine;

import org.jspecify.annotations.NullMarked;
