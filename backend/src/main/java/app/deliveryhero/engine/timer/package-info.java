/** Timers, which fire as commands into a session's queue (LLD section 5.4.2). Null-checked by NullAway. */
@NullMarked
package app.deliveryhero.engine.timer;

import org.jspecify.annotations.NullMarked;
