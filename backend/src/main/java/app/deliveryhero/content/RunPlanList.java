package app.deliveryhero.content;

/** The list a run plan entry belongs to: the practice list or one of the phases (document 10, section 7.4). */
public enum RunPlanList {
    PRACTICE,
    PLANNING,
    DEVELOPMENT,
    TESTING,
    RELEASE
}
