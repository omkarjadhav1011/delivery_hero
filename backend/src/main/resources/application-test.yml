# Automated tests (Setup Guide section 8.2). Testcontainers provides the database; API documentation is on for the
# OpenAPI check (SG-05).
springdoc:
  api-docs:
    enabled: true
  writer-with-order-by-keys: true

dh:
  admin:
    # The public local-only hash of "DHAdmin" (SG-02), as in application-dev.yml
    password-hash: "$2b$12$EJG9VVIEQaFE9x5R9ogZ8OB.S39vZIHig8jHnKly/aCzlhj69z3IW"
  public-base-url: http://localhost:8080
