# Doc issues

Problems found in `docs/`, never fixed there. Format: `planning/CONVENTIONS.md`, section 6.

| ID | Document and section | Issue | Suggested fix | Blocks | Status |
|---|---|---|---|---|---|
| DI-01 | 16 Deployment Guide, Appendix B (`deploy.yml`, line 955) | The repository's `.github/workflows/deploy.yml` now also ignores `planning/**` and `.claude/**`, so planning scripts and Claude Code harness changes don't start a production deploy run (approved by the owner on 2026-09-24; see also DI-02 and DI-03). The appendix's copy lists only `docs/**` and `**/*.md`. Appendix B says the repository wins, so nothing is wrong in practice | At the next revision of document 16, add the two paths to Appendix B's copy of `deploy.yml` | none | Open |
| DI-02 | 16 Deployment Guide, section 10.1 (line 344) | Says the deploy workflow runs on every push "except documentation-only changes"; since DI-01 it also skips pushes that change only `planning/` or `.claude/` | Owner approval needed: changes docs/. Reword section 10.1 to name the two paths, with a version bump and revision row (document 13, section 8.3) | none | Open |
| DI-03 | 01 Charter, Appendix A (DEC-183); 13 Coding standards, section 13 (GS-03) | Both say only documentation-only merges skip the deploy. Skipping planning and harness merges changes that decision, so it needs a new DEC that revises DEC-183 (CLAUDE.md, Workflow) | Owner approval needed: changes docs/. Record it with `/decision` (the next free DEC number), and align GS-03's wording | none | Open |
| DI-04 | 01 Charter, A-04; 16 Deployment Guide, sections 5, 6.1 and 14 (DG-09) | The fallback host disagrees: A-04 names Render (backend) and Neon (database); section 14 says any Linux VM with Docker. Neither says Oracle sign-up needs a card, and no card-free host is named. The owner has no card (OA-03 Blocked), so there's no production host today | Owner decision (Q-01), recorded as a new DEC that revises DEC-58 and DG-09; then align A-04 and document 16, sections 5, 6 and 14. Owner approval needed: changes docs/ | EN-02, OA-03 to OA-21, every OPS check, LT-01, the trial run | Open |
| DI-05 | 16 Deployment Guide, section 7.3, and `deploy/scripts/deploy.sh` line 84 | `deploy.sh` copies the live folder to `/opt/delivery-hero.previous`, but section 7.3 creates only `/opt/delivery-hero` and `/opt/delivery-hero-staging`. `/opt` belongs to root, so the `deploy` user can't create it and the first real deploy fails | Add `/opt/delivery-hero.previous` to the `install -d` line in section 7.3. Owner approval needed: changes docs/ | EN-02 (first deploy) | Open |
| DI-06 | 03 SRS, section 3.2 | In a 3-minute round the incident may start at 2:20 and, with its 20-second limit, run to 2:40, past the 2:30 freeze; section 3.2 says it always ends before the freeze | Owner decision (Q-02): cap the latest start at the freeze minus the incident's limit, or reword; record a DEC | EN-05, US-33 | Open |
| DI-07 | 04 User Stories, section 8 (end-of-S0 checkpoint) | The capacity formula (S0 points / working days x about 10) gives about 65 points against 129 open Must points even if S0 goes exactly to plan, because S1 plans 80 points in 5 days | Owner decision (Q-03) on how velocity is measured | CP-S0 | Open |
| DI-08 | 04 User Stories, section 9 | The dependency graph misses orderings needed to build or test: US-49 before US-59 and US-60; US-37 before US-39 and US-40; a game to join (US-59) before the S0 walking skeleton; a bot-free test game (US-63, Should) before EN-07, OPS-08 and OPS-09 (Must); AC-US17-01 needs US-40; AC-US56-04 needs US-68; AC-US39-04 needs US-09 (Could) | The plan adds these orderings (marked `*` in `research/traceability.md`); a later revision of document 04 could add them | none (handled in the plan) | Accepted |
| DI-09 | 08 LLD, sections 5.8 and 5.9; 03 SRS, SD-10 (DEC-103) | The deploy lock covers Lobby to Reveal only, so a merge during Results restarts the backend and players lose their review and hero cards (DEC-142 covers the message). Neither document says how the host-side deploy script reaches the internal lock endpoint | Owner decision (Q-04) on Results; the plan follows `deploy.sh` for how the lock is read | US-68 | Open |
| DI-10 | 12 UI/UX, P-08, P-10 and section 5.5; 03 SRS, FR-040 | FR-040 shows the streak count from 2; P-08 shows `[1]`. Section 5.5 shows the flame at 3 or more; P-10's top bar shows it at `[2]` | Owner decision (Q-05) | US-30, US-32 | Open |
| DI-11 | 09 Architecture, section 12; 09 QA-06; CLAUDE.md | Section 12 says answer data never reaches phones before Results; QA-06 and CLAUDE.md say before the round ends. Phones get RESULTS only after the winner (DEC-77), so building to "before Results" satisfies both | Align QA-06 and CLAUDE.md on "before Results" at the next revision | none (build to the stricter rule) | Open |
| DI-12 | 08 LLD, section 5.4.4 | The answer pseudocode accepts only LIVE or FROZEN and the current task key, which would reject practice answers (allowed by 5.4.3) and incident answers. The PlayerState diagram lacks `awaitingIssue`, `done`, `seq` and `lockoutSeq` | Build from the prose in sections 5.4.3 to 5.4.6; fix the pseudocode at the next LLD revision | none (build to the prose) | Open |
| DI-13 | 08 LLD, section 5.4.7 (`RoundTimeline`) and 5.13 | `RoundTimeline` hard-codes the freeze at L - 30 s, but the e2e profile needs 10 s (DEC-197) through `dh.game.freeze` | Read the freeze from configuration; fix the LLD text at its next revision | none (build from configuration) | Open |
| DI-14 | 02 PRD, section 7.3; 01 Charter, DEC-87 | The lifecycle diagram allows cancel only from Created, Lobby, Live and Frozen; DEC-87 allows cancel at any point before Results. The decision log wins | Build DEC-87; update the PRD diagram at its next revision | none (the decision log wins) | Open |
| DI-15 | 02 PRD, section 8; 01 Charter, DEC-79 | Auto-close runs 24 hours after the round ends, but no rule covers a game left in Ended because the reveal never started | Owner decision (Q-06) | US-66 | Open |
| DI-16 | 02 PRD, PD-10 (DEC-82); 03 SRS, BR-13 | The readiness warning (fewer scored tasks than round seconds / 6) asks for 100 tasks in a 10-minute round, more than the whole 74-task pool | Owner decision (Q-07); build the rule as written until then | US-58 | Open |
| DI-17 | 14 Test Plan, section 7.4; 15 Test Cases, section 9 (E2E-06) | The suite is expected to take about 11 minutes, so E2E-06 moves to on-demand runs from day one; no document says how its primary criteria (TC-US05-02, TC-US20-02, TC-US42-01) are run and recorded before the go/no-go. E2E-06 also uses a 100-second round, while DS-03 is 60 s and document 14 says 2 minutes | The plan schedules an on-demand E2E-06 run before the load test and records it; align the round lengths at the next revision | none (scheduled in the plan) | Open |
| DI-18 | 11 API, sections 5.2, 5.3, 6.2 and 8.4 | Section 8.4 says every answer gets FEEDBACK or ANSWER_REJECTED, but 5.3 silently drops answers over 5 per second; the ANSWER_REJECTED reasons aren't defined; `DEPLOY_LOCKED` (423) is returned by no endpoint; logout is marked Session only, though 5.2 needs CSRF on every state-changing admin request | Rate-limited answers get no reply; define each reason; name the operations returning `DEPLOY_LOCKED`; mark logout Session + CSRF | none | Open |
| DI-19 | 03 SRS, FR-092 and SD-11; 01 Charter, DEC-104; CLAUDE.md | FR-092 and SD-11 keep names, answers and the password out of logs; CLAUDE.md also names tokens and projector keys | Build to CLAUDE.md's list; align FR-092 at the next SRS revision | none (build to the stricter rule) | Open |
| DI-20 | 03 SRS, FR-005 and section 3.1 | FR-005 says joining is allowed in Lobby and Practice; section 3.1 and FR-012 also allow Countdown and Live until the freeze | Build section 3.1; align FR-005 | none | Open |
| DI-21 | 12 UI/UX, section 10 | Section 4 says the copy deck lists every string, but it lacks some, for example "Wrong! -100", "Wrong! -80", "2 selected", "PRACTICE ROUND", the projector feed lines, phase labels and most admin labels, and the admin navigation's accessible name ("Admin sections", S0-03) | Word missing strings in the copy deck's style in `src/copy.ts`, and list them for the owner's review before the content freeze | US-31, US-33, US-51 (wording only) | Open |
| DI-22 | 13 Coding Standards, sections 5.1 and 9.5, Appendix F | actionlint is a listed merge check but Appendix F has no actionlint step; the section 9.5 diagram's path filters don't match the workflow (`contracts/`, `seed/`); section 9.5 says Playwright's browser is cached, but Appendix F has no cache step (the owner chose on 2026-09-25 to leave it uncached, S0-02) | The actionlint step is in `ci.yml` (S0-02 T2, pinned by digest). At the next revision: add it to Appendix F, fix the diagram, and drop the browser-cache claim from section 9.5 | none | Open |
| DI-23 | Several documents, document control | Stale "Depends on" versions (for example 05 cites SRS v1.0 and Charter v1.2; 09 cites Charter v1.6; 11 cites Charter v1.8; 14 cites SRS v1.3); Charter section 12 still lists document-approval milestones that are already met; A-07 is still "Proposed" | Refresh at each document's next revision | none | Open |
| DI-24 | 15 Test Cases, DS-03 and section 9; 14 Test Plan, section 13 | The end-to-end specs create the `e2e-mini` plan (DS-03) through the admin run-plan API, which US-57 builds in S2, yet the walking-skeleton end-to-end test runs in S0 and the golden path for multiple choice and yes/no in S1 | Until S2-09 builds the run-plan API, the S0 and S1 specs use the seed's Quick 3-minute plan, then switch to DS-03 | none (handled in the plan) | Accepted |
| DI-25 | 15 Test Cases, LT-01 run 3; 14 Test Plan, section 7.6 (TP-03); 08 LLD, section 5.12; 01 Charter, DEC-34 | The headroom run uses 150 players, but a game holds at most 100 (DEC-34), so players 101 to 150 get `GAME_FULL` | Owner decision before Tue 13 Oct: measure headroom with 100 players plus extra projector and admin connections, or allow more players only in the load-test game (a new DEC) | S2-27 (run 3 only) | Open |
| DI-26 | 13 Coding Standards, section 6.2; `.claude/rules/backend.md` | Say every package is `@NullMarked`, but DEC-175 and `/scaffold-en01` run NullAway only in `engine` and `scoring`. With `OnlyNullMarked`, marking every package would extend NullAway everywhere. The scaffold follows DEC-175 (approved by the owner on 2026-09-25, S0-01 T1) | Owner approval needed: changes docs/. Reword section 6.2 to limit `@NullMarked` to `engine` and `scoring`, and update the rules file | none | Open |
| DI-27 | 08 LLD, section 5.13 | `GameProperties` has no keys for DEC-197's e2e timings (60-second rounds, a fixed random seed). The scaffold adds `dh.game.min-round-length` and `dh.game.random-seed` (see also DI-13) | Owner approval needed: changes docs/. Add the two keys and their defaults to the table in section 5.13 | none | Open |
| DI-28 | 08 LLD, section 6.6 | Places the DEC-111 browser targets in `next.config`, but Next.js 16 reads them from `browserslist` in `package.json`, which is where the scaffold puts them | Owner approval needed: changes docs/. Say `package.json` `browserslist` in section 6.6 | none | Open |
| DI-29 | 12 UI/UX Wireframes, section 5.2 | Names the color tokens `--bg` and so on, but Tailwind CSS 4's `@theme` needs the `--color-` prefix (`--color-bg`), which the scaffold uses | Owner approval needed: changes docs/. Note the Tailwind name beside each token in section 5.2 | none | Open |
| DI-30 | 13 Coding Standards, Appendix E (`.github/dependabot.yml`) | Majors are ignored only for Maven and npm, so Dependabot proposes major bumps of Docker base images that the architecture document fixes, for example `eclipse-temurin` 21 to 25 (pull request #1, 2026-09-25) while Java 21 is decided (architecture document, section 9.1) | Owner approval needed: changes docs/. Add `ignore: [{dependency-name: "*", update-types: [version-update:semver-major]}]` to the Docker entry in Appendix E and `dependabot.yml`; meanwhile close such pull requests with `@dependabot ignore this major version` | none | Open |
| DI-31 | 16 Deployment Guide, Appendix B.6 and section 10.1 | The deploy build re-checks the backend fully: `./mvnw -B package` fails on a failing test, a Spotless violation or an Error Prone error. For the frontend it runs only `npm test` and `npm run build`, so a merged change with a formatting, lint or type error still deploys (proved locally in S0-02 T7). DEC-181 calls the deploy build the backstop for those failures, because GitHub Free can't block merges | Owner approval needed: changes docs/ and `deploy.yml`. Add `npm run format:check && npm run lint && npm run typecheck` before `npm test` in B.6 and `deploy.yml` | none | Open |
| DI-32 | 12 UI/UX Wireframes, sections 9 and 10 | The admin brand reads "DELIVERY HERO · Admin" on A-01 and "DELIVERY HERO admin" on A-02, and the copy deck has neither; it also has no row for the phone home page ("Scan the QR code on the big screen", from LLD section 6.1 only). `src/copy.ts` copies each wireframe, so the two admin screens differ | Owner approval needed: changes docs/. Settle one admin brand wording and add it and the home string to the copy deck in section 10 | none | Open |
| DI-33 | 08 LLD, section 6.1 | The project structure doesn't list the screen shells EN-08 adds (`src/player/PhoneShell.tsx`, `src/screen/ProjectorShell.tsx`, `src/admin/components/AdminShell.tsx`) or `src/ui/icons/` (the Pixelarticons path data and its MIT license) | Owner approval needed: changes docs/. Add them to the tree in section 6.1 | none | Open |
| DI-34 | 12 UI/UX Wireframes, section 5.2 | The contrast table has no rows for `--primary` or `--accent` as text on `--bg` or `--surface`, which the brand, the timer number and the admin navigation use; the icon rows (6.82 to 12.06 on `--bg`) suggest they pass 4.5 | Owner approval needed: changes docs/. Add the text pairings with their measured ratios | none | Open |
| DI-35 | 11 API Specification, section 6.3 | No issue code for a run plan key that names no task (SRS 7.4: "every key referenced by a run plan must exist"). `ContentValidator` uses `UNKNOWN_KEY` (S1-01), which the admin API will return in `VALIDATION_FAILED` once run plans can be saved (S2-09) | Owner approval needed: changes docs/. Add `UNKNOWN_KEY` (Error, "A run plan names a task key that doesn't exist") to the table in section 6.3 | none | Open |
| DI-36 | 08 LLD, section 5.6 (StompEventListener) | Says the broker closes a connection that stops sending heartbeats within 20 seconds (DEC-122). Spring's simple broker waits three heartbeat intervals, 30 seconds at 10-second heartbeats, so `HeartbeatWatchdog` in `realtime` closes connections silent for 19 seconds instead. The same section names `SessionSubscribeEvent` for `ClientSubscribed`; that event can fire before the broker has registered the subscription, so the gateway uses an `ExecutorChannelInterceptor` that fires after the broker has (LD-08) | At the next revision of the LLD, say the gateway's watchdog, not the broker, closes silent connections, and that `ClientSubscribed` follows the broker's registration of the subscription. Owner approval needed: changes docs/ | none | Open |
| DI-37 | 11 API specification, section 8.2 | "Any other SUBSCRIBE or SEND is refused with an ERROR frame" gives no `message` code, while section 8.1 names `UNAUTHORIZED` for CONNECT. The gateway sends `FORBIDDEN` | At the next revision of document 11, name `FORBIDDEN` in section 8.2, and say that the connection closes after any ERROR frame (STOMP 1.2). Owner approval needed: changes docs/ | none | Open |
| DI-38 | 08 LLD, section 5.6 (WebSocketConfig) | Says heartbeats of 10,000 ms in both directions. Spring's broker checks for due heartbeats every min(its two values) and writes one only after more than the negotiated interval of silence, so advertising 10,000,10,000 lets the first server heartbeat slip to 20 s. The broker advertises `heart-beat:2000,10000` instead: it checks every 2 s, so the server sends at least every 10 s (SRS 6.3 holds; STOMP allows more often). Because Spring doesn't count its own heartbeat as a write, an idle connection then gets one byte every 2 s until other traffic flows (measured through Nginx on the local stack) | At the next revision of the LLD, give the server's advertised value and why. Owner approval needed: changes docs/ | none | Open |
| DI-39 | 08 LLD, sections 5.4.10 and 5.7 | The session sends the full state when it handles `ClientSubscribed`. Until the engine exists (EN-05), the gateway sends it through a stopgap `CurrentState` port, marked TODO(EN-05) for deletion; it must never read session state from outside the session thread | None if EN-05 removes the port as planned; otherwise the LLD must change. EN-05's subplan should list this | EN-05 | Open |
| DI-40 | 08 LLD, sections 5.2 and 5.4.3 | `ClientSubscribed(String connectionId, ClientRole role, UUID playerId)` uses `ClientRole` without defining it, and `playerId` has no value for projector and admin connections. The code defines `enum ClientRole { PLAYER, PROJECTOR, ADMIN }` and makes `playerId` `@Nullable` | At the next revision of the LLD, define `ClientRole` and mark `playerId` as set only for players. Owner approval needed: changes docs/ | none | Open |
| DI-41 | 08 LLD, sections 5.6 and 5.13 | `dh.public-base-url` isn't in the configuration table, but the STOMP endpoint now allows exactly its origin (`SiteProperties`), because Spring's same-origin default fails behind Nginx on any port but 443 (`Host $host` carries no port) | At the next revision of the LLD, add `dh.public-base-url` to 5.13 and name it as the allowed origin in 5.6. Owner approval needed: changes docs/ | none | Open |
| DI-42 | 18 Setup Guide, section 6 (local credentials) | The `dev` profile now has a fixed player token and projector key (`dh.dev.player-token`, `dh.dev.projector-key` in `application-dev.yml`, public and local-only per DEC-208) for checking `/ws` before joining exists; the local credentials table doesn't list them. They are marked TODO(US-01) for removal | Add them to the table while they exist, or remove them with US-01. Owner approval needed: changes docs/ | none | Open |
