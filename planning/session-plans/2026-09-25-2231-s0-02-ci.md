# Session 2026-09-25 22:31: S0-02 CI merge checks and the deploy workflow

## Context

- Focus: S0-02 (Must, 0 points), eligible now that S0-01 is merged; phase S0, `/dh` build mode.
- Constraint from the owner (2026-09-25): keep everything local. Deployment is on hold (DEC-213 draft stashed on `docs/q01-linux-vm`).
- Reading mode: sections through `section.py`.
- Branch: `chore/ci-merge-checks` from `main` at e96ee0b.

## Report

- Local now:
  - T1: align `ci.yml` with Appendix F and section 9.5, checked by diff.
  - T2: add a pinned actionlint step, run locally in Docker, with a bad `needs:` probe.
  - T3: pin check.
  - T4: Dependabot against Appendix E.
  - T5: JaCoCo gate probe.
  - T7: `deploy.yml` against B.6, with its build commands run locally on failing probes.
- Needs GitHub, so deferred: the runtime proofs for T1 (docs-only run, cancel on a new push), T2 (a red run) and T6 (a scratch pull request with four red runs). Those stay open.
- Owner: T8 (Monday Actions check) and T9 (DI-22 doc fix approval).

## Choice

- 2026-09-25: owner approved the local-only plan; stop before changing `ci.yml` to show the diff.

## Result

Filled in at the end.
