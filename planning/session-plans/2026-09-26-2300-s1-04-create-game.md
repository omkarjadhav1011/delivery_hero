# Session 2026-09-26 23:00: S1-04 create a game with its own snapshot

## Context

- Focus: S1-04 (US-59, US-54; Must, 6 points): snapshot, game creation, state recorder, admin game endpoints, New game screen, end-to-end fixtures, the local walking-skeleton demonstration (T7), removing the dev STOMP credentials (T8) and AC-US19-02 (T9).
- State: /dh state 9 (Ready), phase S0 day 3 of 6, mode build. S2-08 is In review (PR #28, waiting for the owner to merge).
- Sources: document 05 US-59, US-54; LLD 5.8, 5.12; API 7.7; document 10 sections 7.5, 8.5; document 12 A-08.
- Branch: `feat/us-59-create-game` from `main`.

## Report

- Plan: 75 of 483 tasks, 23 of 230 points (Must 23 of 155); 49 of 271 criteria passing, none failing.
- `next.py` lists S0-03, S1-04 and S2-09. S0-03 has no Claude work left (T1 owner pick, T9 MAN-02 manual check).
- S1-04 unblocks S1-06 and S1-07, which now hold the S0 walking-skeleton demonstration (PC-09). S2-09 (which unblocks S2-08 T6) comes after.
- PC-09 (approved 2026-09-26, commit 3c4f079): T6 and T7 moved to S1-07 T9 and T10, because joining needs `OPEN_LOBBY` (S1-05 T7, S1-07 T2); T10 added, a read-only `GET /api/admin/run-plans` for the plan picker. The S0 walking-skeleton milestone now waits on S1-05 and S1-07.
- Owner: Q-02 and Q-03 due Tue 29 Sep (Q-03 decides how CP-S0 is measured); S0-02 T8 Actions usage check starts Mon 28 Sep; S0-03 T1 and T9; OPS-21 testing due Tue 29 Sep. No owner actions due in the next 7 days (the production actions wait on Q-01, Mon 12 Oct).
- Attention: S1-03, S2-07 and S2-08 started before their dependencies were Done (recorded in the previous session plan).

## Choice

- Approved 2026-09-26: run it all, in the order below.
- Proposed order, one commit per task (`wip(S1-04): T<n> ...`):
  1. T3 `GameStateRecorder` (test first: `GameStateRecorderTest` ordered writes).
  2. T1 `SnapshotFactory` and `GameSnapshot` (test first: `GameLifecycleIT` AC-US54-01, AC-US54-02).
  3. T2 `GameLifecycleService.create`, `GameEntity`, code and projector key, projector key registered after commit, `ANOTHER_GAME_OPEN` (test first: `GameLifecycleIT` AC-US59-01 to 03).
  4. T9 game keeps its length (test first: `GameLifecycleIT` AC-US19-02).
  5. T10 read-only run plan list (test first: `RunPlanListIT`).
  6. T4 `GameController` (test first: `GameLifecycleIT` AC-US59-01 over REST), plus `docs/openapi.json`, `contracts/` and `frontend/src/types`.
  7. T5 New game screen (test first: `NewGame.test.tsx`).
  8. T8 remove `DevCredentials` (test first: `StompConnectionIT` on real credentials); the document 18 section 6 edit is asked for separately.
- Assumptions: `GameSnapshot` records live in `content` (the LLD names no package; `engine` and `lifecycle` both read them, and `lifecycle` already depends on `engine`). `allowedActions` for CREATED is `["OPEN_LOBBY", "CANCEL"]` from API 7.7 until S1-07 T1. `warningCount` is 0 until S2-23.
- Checks: `./mvnw spotless:apply` and `./mvnw -B verify`; frontend format, lint, typecheck, tests and build; `/e2e` `join-and-lobby`; a local `curl` for 201 then 409. Reviewers: `backend-reviewer`, `frontend-reviewer`, `spec-guardian`, `security-reviewer` (admin endpoints, projector key).

## Result

- Pending.
