# Session 2026-09-26-0034: S0-04 real-time channel

## Context

- Focus: S0-04 (EN-04), T1 to T8.
- State: 9, Ready. PRs #9 to #12 were merged, but the tracker still showed them open, so it was synced first (0f9b14a).
- Reading mode: the subplan's Context to load sections, plus LLD 5.4.3 and 6.2.
- Branch: `feat/en-04-realtime-channel` from `main` at 8b7e483.

## Report

- Phase S0, day 3 of 6, build mode. Points 0 of 230 Done: nothing reaches Done until production exists (Q-01).
- Eligible: S0-02, S0-03 (open owner and art tasks) and S0-04. S0-04 is the next step on the skeleton path (EN-04, then US-01, US-02 and US-04).
- Owner items: Q-01 is due today, and a draft answer (DEC-213) sits on the unpushed branch `docs/q01-linux-vm`. OA-03 to OA-21 are blocked on it. Q-02 and Q-03 are due Tue 29 Sep.

## Choice

- 2026-09-26: the owner chose "Sync tracker, then S0-04".

## Result

- The owner approved the session plan; T1 to T8 are done, followed by the review fixes. The commits on `feat/en-04-realtime-channel` run from 0f9b14a (tracker sync) to the wrap-up commit.
- Backend: `/ws` with a simple broker, `HeartbeatWatchdog`, `TokenService`, the principals, `StompAuthInterceptor`, `DestinationPolicy`, `StompEventListener`, the gateway commands in `engine.command`, `SiteProperties` and `DevCredentials` (dev only). `./mvnw -B verify`: 64 unit and 30 integration tests pass, and the coverage gates are met.
- Frontend: `reconnect.ts`, `stompClient.ts`, `useStomp.ts` and the P-19 copy. Format, lint, typecheck, 78 tests and the build pass.
- `ac_coverage.py` now counts AC-EN04-01 to AC-EN04-04 as automated and passing: 15 Must criteria automated, up from 11.
- Local stack through Nginx on port 8090: the own origin gets 101 and a foreign one 403. The first heartbeat comes at about 10.8 s, then one every 2 s while idle. A bad token gets ERROR `UNAUTHORIZED`.
- Not done: no pull request yet (awaiting the owner). Document changes DI-36 to DI-42 need the owner's approval. `RealtimeController` belongs to S1-08 and S1-02.
- Actuals: 00:34 to about 02:40 (about 2 hours). Main session tokens are estimated at about 450k; subagents used about 274k (three reviewers).
