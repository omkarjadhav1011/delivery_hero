# Session 2026-09-25 23:38: S1-01 seed loader and round-length rules

## Context

- Focus: S1-01 (Must, 4 points), pulled forward by PC-01 because CI's e2e job fails at the seed step until the loader exists. Phase S0, `/dh` build mode. Its only dependency, S0-01, has every task ticked and its code on `main`.
- Reading mode: the subplan's "Context to load" sections through `section.py` (LLD 5.2, 5.3, 5.8, 5.10, 5.12, 5.14; SRS 7.3, 7.4; document 10, 7.3 and 8.4; document 15, section 6; AC-US56).
- Branch: `feat/us-56-seed-loader` at 15d6fcd.

## Report

- This session: T1 to T6, one commit per task.
  - T1: `ContentValidator` and `ValidationReport` in `app.deliveryhero.content`, with `ContentValidatorTest` (AC-US19-01 and the SRS 7.3 rules).
  - T2: `SeedFile` records and `SeedImporter` validate the whole file first. Checked by `SeedImportIT` AC-US56-02 against a DS-08 fixture in `backend/src/test/resources/seed/`.
  - T3: JPA entities and repositories for the content tables, then a one-transaction upsert. Checked by AC-US56-01 (74, 4, 2) and AC-US56-03.
  - T4: lock check on games in LOBBY through REVEAL. Checked by AC-US56-04.
  - T5: the seed context has no cleanup beans. `SeedModeTest`'s stub test goes away.
  - T6: run it twice on the local stack, plus `validate_seed.py`.
- Blocked and left open: T7 (needs S1-04 game creation) and T8 (OA-20, needs Q-01).
- Assumptions, from the subplan and the documents:
  - The lock refusal exits with status 1 and prints the `DEPLOY_LOCKED` message (subplan risk 2).
  - The seed file must contain all four roles once, each with exactly 3 correct and 3 wrong lines. This matches `validate_seed.py` and the V1 check constraints.
  - A missing `timeLimitSeconds` is stored as empty (document 10, 8.4). Explanations are required for scored and incident tasks.
  - Readiness warnings (BR-13) stay out of the loader.
- Checks: `./mvnw spotless:apply`, `./mvnw -B verify` (Docker is running), `validate_seed.py`, the local seed run twice, then `backend-reviewer` and `spec-guardian`.
- Owner work due: OA-03 to OA-21 are all blocked on Q-01 (answer due Sat 26 Sep).

## Choice

- 2026-09-25: owner approved running T1 to T6 without asking per task.

## Result

- Commits on `feat/us-56-seed-loader`, not pushed: dffffaa (T1), 6d3b2e6 (T2), 31d836e (T3), 9082c7f (T4), bcd5d27 (T5), 7e5d990 (T6) and 19d9f94 (review fixes).
- Files: in `common`, the four content enums and `GameState`. `config/ClockConfig`. In `content`, the validator, definitions, content records, entities and repositories. In `seed`, `SeedFile`, `SeedImporter`, `SeedWriter` and `SeedCommand`. `lifecycle/GameInProgressCheck`. The DS-08 fixture. Tests: `ContentValidatorTest` (20) and `SeedImportIT` (14).
- Criteria now passing: AC-US56-01 to 04 and AC-US19-01. `./mvnw -B verify` is green, the local seed runs twice cleanly, and `validate_seed.py` reports 0 errors.
- Changes from the plan's assumptions, after spec-guardian: the loader doesn't require all four roles, and it leaves the BR-13 list errors to the readiness check. Issue codes follow document 11, section 6.3. DI-32 records the missing `UNKNOWN_KEY` code.
- Not done: T7 (waits for S1-04) and T8 (OA-20, waits for Q-01).
- Incident: a `docker compose run` without `DH_LOCAL_PORT=8090 DH_LOCAL_DB_PORT=5433` recreated the local Postgres container on 5432, which the Windows PostgreSQL service holds. It was restored with the recorded ports, and the data volume was untouched.
