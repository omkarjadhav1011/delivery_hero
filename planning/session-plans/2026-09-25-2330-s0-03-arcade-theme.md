# Session 2026-09-25 23:30: S0-03 Arcade theme and screen shells

## Context

- Focus: S0-03 (EN-08, Must, 3 points); phase S0, `/dh` build mode. Target date Sat 26 Sep.
- Dependency: S0-01 is merged (PR #9, e96ee0b) but still In progress in the tracker, because deploy verification waits on production (OA-03 is blocked). S0-02 started on the same basis.
- Constraint from the owner (2026-09-25): keep everything local; nothing is pushed.
- Reading mode: sections through `section.py`.
- Branch: `feat/en-08-arcade-theme` from `main` at e96ee0b. S0-02 stays paused on `chore/ci-merge-checks`.

## Report

- Owner first: T1, which means choosing the character pack and confirming the icon set, each with its license.
- Now, without T1:
  - T3: `theme.test.ts` AC-EN08-01 checks the tokens against section 5.2 and the display font against 1 rem. Then the theme: reduced motion and `rem` sizes.
  - T4: the UI kit (`ArcadeButton`, `SpeechBubble` without art, `TimerBar` visuals, `CodeBlock`, `Badge`, `Modal`, `PixelIcon`), each with tests first.
  - T6: the phone, projector and admin shells, with landmark and heading tests. They replace `PlaceholderPage`.
  - T7: `security-privacy.spec.ts` AC-EN08-02, which checks that every request stays on the game's own address.
  - T8: `accessibility.spec.ts` AC-EN08-03, at 320 px and 200% text.
- After T1:
  - T2: the README credits and license files.
  - T5: the character art.
  - If the pack isn't chosen, the shells use text-only placeholders and T5 stays open (R-11).
- T9 (MAN-02) is the owner's visual check. It is recorded in `test-results/manual-results.csv` once the owner has looked.
- Checks: `/check` for the frontend (format, lint, typecheck, tests with coverage, build), `/e2e` for the two specs on the local stack (ports 8090 and 5433), markdownlint on the README, and then `frontend-reviewer` and `spec-guardian`.

## Choice

- 2026-09-25: owner approved running S0-03 locally from `main` (nothing pushed). T1: Pixelarticons (MIT, confirmed at the source) for the icons; the characters stay text-only placeholders for now, so T5 stays open (R-11).

## Result

- Pending.
