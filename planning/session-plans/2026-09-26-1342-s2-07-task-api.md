# Session 2026-09-26 13:42: S2-07 task editor, backend

## Context

- Focus: S2-07 Task editor with preview (US-51, Must, 8 points), pulled forward from S2 because it is the first eligible build subplan; production work waits on Q-01, OA-03 and OA-04.
- State: /dh state 9 (Ready), phase S0, mode build. Sections read with `section.py`: 08 5.3, 11 6.2, 6.3, 7.4, 9.1, 10 7.2, 8.3, 03 7.3, 05 7.10, DEC-74.
- Branch: `feat/us-51-task-editor` from `origin/main` (59a34e2).

## Report

- Much of T1 already exists from S1-01: `ContentValidator` has `EXACTLY_ONE_CORRECT`, `ORDER_SAME_AS_CORRECT`, `MARKED_WORDS_COUNT` and `PROMPT_OVER_25_WORDS`. T1 becomes the AC-US51-02 tests plus message wording (the document 11 example says "Choose exactly one correct option.").
- `ApiErrorCode` lacks `VALIDATION_FAILED`, `NOT_FOUND` and `TASK_IN_USE`; the Problem body needs an `errors` list.
- There is no `PublicTaskView` yet. T5 creates the one mapping that the engine will reuse (LLD 5.3, DEC-130).
- `frontend/src/player/tasks` is empty. The player task components come from S1-11, S2-12 and S2-13 (all Not started), so T7 can't reuse them yet.
- Assumptions: `MISSING_EXPLANATION` is returned as a save warning for scored and incident tasks (document 11, section 6.3 lists it as a warning; SRS 7.3 calls it a readiness warning). The `EDIT_CONFLICT` check stays with S2-08 (US-53).

## Choice

- Approved 2026-09-26 13:50: T1 to T5 (backend) in this session, one PR. T6 in the next session. T7 blocked until S1-11, S2-12 and S2-13 (plan change approved).

## Result

- Commits: 24121c7 (T1), d9bbd59 (T2), 977d10e (T3), a20b676 (T4), cb50146 (T5), bb984ef (frontend types), 7004279 (review fixes), then the OpenAPI document; PR #24.
- Files: `content/` (ContentValidator, TaskDefinition, TaskService, TaskInput, TaskDetail, PublicTaskView, entities, RunPlanRepository), `api/admin/` (TaskController, AdminRequestProblems), `common/` (ApiErrorCode, DeliveryHeroException), `api/ProblemHandler`, `seed/SeedImporter`, tests, `frontend/src/types/dto.ts`, `docs/openapi.json`.
- Not done: T6, which is next. T7 is blocked on S1-11, S2-12 and S2-13 (approved plan change). New doc issues: DI-58 to DI-60.
- Also this session: PR #23 re-lands the DHAdmin change on main (PR #22 had merged into a branch that was already merged).
