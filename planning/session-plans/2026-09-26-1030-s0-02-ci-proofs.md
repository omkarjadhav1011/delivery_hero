# Session 2026-09-26-1030: S0-02 CI merge checks, the GitHub proofs

## Context

- Focus: S0-02, T1, T2, T4 and T6. T8 and T9 are owner tasks.
- State: 9, Ready. First eligible subplan from `next.py`. S0-05 was set to Done first (e8f0e3d): PR #14 merged, and the production check follows the first deploy.
- Reading mode: the subplan, document 13 sections 9.5 and Appendix E and F.
- Branch: `chore/ci-merge-checks`, merged in PR #10 and now 57 commits behind `main`. Work goes on a fresh branch from `main`, `ci/s0-02-proofs`.

## Report

- Phase S0, day 3 of 6. Tasks 44/484, points 8/230, Must 8/155. Criteria: 25 of 271 passing.
- CP-S0 due Tue 29 Sep (Q-03 open). OA-03 and OA-04 overdue, and every production step waits on Q-01.
- The Deploy workflow fails on every merge to `main` at the rsync step, because `DEPLOY_HOST` is empty (OA-17). That's expected until a host exists.
- Already on `main` from PR #10: path filters in the `changes` job, `concurrency` with `cancel-in-progress`, Maven and npm caches, and the pinned actionlint step.
- Gap: no Playwright browser cache (section 9.5).
- Evidence already in hand for T1: PR #15 (planning files only) ran `changes` and `repo-checks`, and `backend`, `frontend` and `e2e` were skipped (run 36217912360).

## Planned approach

- **T1:** add an `actions/cache` step for `~/.cache/ms-playwright`, keyed on the Playwright version in `package-lock.json`, pinned to a full SHA. Run `npx playwright install` only for system dependencies on a cache hit. Prove it:
  - PR #15 already shows the filters.
  - The e2e job on this branch's PR shows a cache save, and a re-run shows a hit.
  - Two quick pushes show the first run cancelled.
- **T2:** run actionlint locally on both workflows. Then push a scratch commit with a bad `needs:` to a scratch PR, see `repo-checks` fail, and revert.
- **T4:** diff `.github/dependabot.yml` against Appendix E. Check that Dependabot's action PRs bump SHAs, not tags. None are open now, so check its configuration and any closed ones.
- **T6:** one scratch PR, `test/s0-02-merge-gate`, never merged. It gets four red runs, each reverted: a failing test, a Spotless violation, an Error Prone error, and a bad title. Record the run links in the progress log, then close the PR.
- **Checks:** actionlint locally, the `ops-reviewer` subagent on the `ci.yml` change, then `/progress`.

## Approval points

- Pushing `ci/s0-02-proofs` and opening its PR.
- Pushing the scratch branch and opening, then closing, the scratch PR for T2 and T6. This uses about 4 to 6 CI runs of Actions minutes (DEC-183).
- Updating PR #15 with e8f0e3d (S0-05 Done).

## Result

To be filled in at the end.
