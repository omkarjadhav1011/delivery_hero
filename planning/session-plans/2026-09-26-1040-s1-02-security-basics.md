# Session plan: S1-02 security basics

## Context

- Focus: S1-02 (EN-06), headers, CSP, CSRF and rate limits.
- State: `/dh` state 9, phase S0 in build mode. S0-03 is waiting on owner task T1, so S1-02 was the only subplan with work Claude could do.
- Reading mode: sections through `section.py` (LLD 5.6, 5.9, 6.6; API 5; document 16 B.4, B.5; document 05 EN-06; document 15 E2E-07).
- Branch `feat/en-06-security-basics`, from `origin/main` at 2f70f41.

## Report

Already in the scaffold, checked this session:

- T5: the Nginx template and `routes.conf` match document 16 B.4 and B.5 byte for byte. The local stack (port 8090) sends the CSP, `nosniff`, `no-referrer` and `frame-ancestors 'none'` on pages and API responses.
- T6: `csp-hashes.mjs` exists, runs as `postbuild`, and both images copy its output (`deploy/local/nginx.Dockerfile`, `deploy.yml`).
- T2, frontend half: `http.ts` already sends `X-XSRF-TOKEN` from the `XSRF-TOKEN` cookie.
- T7: ESLint `react/no-danger` is already `error`.
- Found: `HEAD /health` and `HEAD /api/games/X` return 401, because the rules permit GET only.
- The answer path doesn't exist yet: there's no `RealtimeController`, `SubmitAnswer` or `AnswerPayload` (S0-04 left the answer mapping to S1-02).

Planned tasks: T1 to T4 and T7 need new code, T5 and T6 need verification only, T8 is the end-to-end spec, and T9 stays blocked.

## Choice

2026-09-26 10:40: the owner chose "Run S1-02" from the `/dh` menu.

## Result


- Commits a0631f4 (T1), 6e6ab66 (T2), cb15962 (T3), ec1e586 (T4), 7c8d706 (T7), c410fd6 (T5, T6, T8), 0dc41d9 (review fixes).
- Files: `security/SecurityConfig`, `RateLimiter`, `RateLimitFilter`; `realtime/RealtimeController`, `AnswerSubmit`; `common/AnswerPayload` and its four records; `engine/command/SubmitAnswer`, `Command`, `GameSession`; the tests `SecurityIT`, `RateLimiterTest`, `RealtimeControllerTest`, `AnswerPayloadTest` and `MutableClock`; frontend `e2e/fixtures/ds05.ts`, `security-privacy.spec.ts` and the `CodeBlock` and `SpeechBubble` tests; DI-49 to DI-51.
- Checks: `./mvnw -B verify` passed (48 integration tests, all unit tests); frontend format, lint, typecheck, 109 unit tests and build passed; e2e on the local stack, 33 passed. gitleaks and actionlint aren't installed here; the seed check passed.
- Not done: T9 (blocked on S2-15 and S2-07). The reviewers' consider-only items are listed in the subplan log.
